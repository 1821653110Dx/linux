# purpose
    install py_cmd_tool
# install
    pipx install {pkg}
# uninstall
    pipx uninstall {pkg}
# upgrade
    pipx upgrade {pkg}
# upgrade for all
    pipx upragde-all
# auto manage PATH
    pipx ensurepath
# cache
	pip cache purge